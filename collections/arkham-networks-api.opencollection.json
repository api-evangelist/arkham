{
  "opencollection": "1.0.0",
  "info": {
    "name": "Arkham Intel Analytics Networks API",
    "version": "1.1.0"
  },
  "items": [
    {
      "info": {
        "name": "Networks",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Get historical data for a blockchain network",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "{{baseUrl}}/networks/history/:chain",
            "params": [
              {
                "name": "chain",
                "value": "",
                "type": "path",
                "description": "The blockchain network identifier."
              }
            ]
          },
          "docs": "Retrieves the full daily price, volume, and market-cap history of a blockchain network's native token, from the token's launch to the latest available snapshot."
        },
        {
          "info": {
            "name": "Get current status for all blockchain networks",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "{{baseUrl}}/networks/status"
          },
          "docs": "Retrieves the current status for all supported blockchain networks, including price, 24h price change, volume, market cap, latest block (tip), total transfers, and gas/fee information. Each network includes an \"asOf\" object giving the time each piece of data was last read; values are served from a cache refreshed about once per minute."
        }
      ]
    }
  ],
  "bundled": true
}